# website
Website da banda.

## Deployment

The site is deployed to GitHub Pages via GitHub Actions. On every push to `main`, the workflow in `.github/workflows/jekyll.yml` builds the site with Jekyll 4 and deploys it.

**Setup:** In the GitHub repo, go to **Settings > Pages** and set the **Source** to **"GitHub Actions"** (not "Deploy from a branch").
